# Server Configuration
# Dynamically binds to the cloud platform PORT environment variable, defaulting to 8080 locally
server.port=${PORT:8080}

# Production Database Configurations
# These will be read from environment variables on Railway/Render/Koyeb
# Default fallbacks are provided for local development
db.url=${DB_URL:jdbc:mysql://localhost:3306/sneakerhub?useSSL=false&allowPublicKeyRetrieval=true}
db.username=${DB_USER:root}
db.password=${DB_PASSWORD:tahir@80882777}
